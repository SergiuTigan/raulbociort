---
const { frontmatter } = Astro.props;
---

<div class="flex">
  <img src=`${frontmatter?.image.src}`></img>
  <div class="flex flex-col">
    <h2>{frontmatter?.name}</h2>
    <p>{frontmatter?.description}</p>
  </div>
</div>
